#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    // check that CLI was used correctly


    // if yes, open the image in question


    // load the image

    // store the information?

    // start searching data from NULL -> END, for the beginning of a jpeg

    // open a new JPEG file

    // keep writing data in chunks of 512 bytes until a new JPEG file is reached

    // close old file

    // repeat

}