
def main()

    while True:
        try:
            # prompt user for input
            card = input("Card Number: ")

    # check for valid card length (13 - 16 digits)
    if (len(card) < 13 or len(card) > 16):
        print("INVALID")

    else:





